# השתמש בתמונה רשמית של Node.js כהורה
FROM node:14

# קבע את תיקיית העבודה בתוך הקונטיינר
WORKDIR /usr/src/app

# העתק את תוכן התיקייה הנוכחית לתוך הקונטיינר
COPY . .

# התקן את התלויות הנדרשות
RUN npm install

# חשוף את פורט 8080 של הקונטיינר החוצה
EXPOSE 8080

# קבע את הפקודה להריץ את האפליקציה
CMD ["npm", "start"]
